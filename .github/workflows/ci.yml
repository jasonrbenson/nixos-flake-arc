name: CI — NixOS Arc Flake

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Check flake
        run: nix flake check --no-build

      - name: Check flake metadata
        run: nix flake metadata

      - name: Show flake outputs
        run: nix flake show

  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Check Nix formatting
        run: nix develop --command nixpkgs-fmt --check flake.nix packages/**/*.nix modules/*.nix

  # Build job — uncomment once package hashes are populated
  # build:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       system: [x86_64-linux]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: cachix/install-nix-action@v30
  #     - name: Build azcmagent
  #       run: nix build .#azcmagent
